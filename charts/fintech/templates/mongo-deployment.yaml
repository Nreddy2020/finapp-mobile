{{- if .Values.mongo.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-mongo
  labels:
    app: {{ .Chart.Name }}-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-mongo
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:7.0
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: {{ .Chart.Name }}-mongo-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Chart.Name }}-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-mongo
spec:
  selector:
    app: {{ .Chart.Name }}-mongo
  ports:
    - port: 27017
      targetPort: 27017
{{- end }}
