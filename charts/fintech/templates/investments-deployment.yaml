apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-investments
  labels:
    app: {{ .Chart.Name }}-investments
spec:
  replicas: {{ .Values.services.investments.replicas }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-investments
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-investments
    spec:
      containers:
        - name: investments
          image: "{{ .Values.global.repository }}/investments-service:{{ .Values.global.tag }}"
          imagePullPolicy: {{ .Values.global.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.investments.port }}
          env:
            - name: MONGO_URL
              value: "mongodb://{{ .Chart.Name }}-mongo:27017"
            - name: DB_NAME
              value: "fintrack"
            - name: SECRET_KEY
              value: "kubernetes-secret-key"
          livenessProbe:
            httpGet:
              path: /docs
              port: {{ .Values.services.investments.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /docs
              port: {{ .Values.services.investments.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-investments
spec:
  selector:
    app: {{ .Chart.Name }}-investments
  ports:
    - port: 80
      targetPort: {{ .Values.services.investments.port }}
