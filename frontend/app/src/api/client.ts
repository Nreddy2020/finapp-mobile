// Deprecated wrapper for apiClient inside the Expo Router `app` directory.
// Non-route utilities must live outside `app` (see src/api/client.ts).
// Do NOT import from "app/src/api/client"; use relative imports to src/api/client instead.

export {};
